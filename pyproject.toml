[project]
name = "partimorph"
version = "0.1.0"
description = "A high-performance Python library for comprehensive particle shape analysis and visualization."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "numpy",
    "scipy",
    "opencv-python",
    "scikit-image",
    "matplotlib",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
